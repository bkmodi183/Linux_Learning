Commands learnt so far:
1. mkdir -> command is used to create a directory
2. touch -> command is used to create a file
3. pwd -> used to check your present working directory
4. whoami -> used to check with which user you are connected with the server
5. uname -> used to check which Kerenel you are using. In our case we are using linux.
7. cd -> used to change the directory
8. cd ..-> used to go back to preveious directory
9. / -> root directory. Everthing begins from here
10. sudo su -> this commands lets you do something as a super user which has the permissions equivivalent to root user
11. su -> used to swith user
12. useradd username -> used to add the users
13. cat -> used to see the content of the file
14. /etc/passwd username -> used to create password for the user
15. sudo cat /etc/passwd -> is used to check the existing user list
16. userdel username -> user to delete the existing user
17. groupadd  groupname -> is user to create group
18. sudo cat /etc/groups -> used to check  exisitng groups in the server
19. groupdel groupname -> to delete the group
20. getacl path(/home/Folder/file.txt) -> this command is used to check what are the permissions for a file directory or a user has.
21. sudo usermod -aG group_name username -> used to add user into the group
22. ls -a -> to see the all folder including hidden
23. ls -l -> to list the content with detailed information including file permission, size, time etc.
24. sudo useradd -m <username> -> here this command is used to create a user and '-m' flag which create creates a userdirectory 
                                  at home.
25. cat /etc/passwd -> to see the user created
26. cat /etc/group -> to see the groups created
27. sudo groupadd <group-name> -> to create a new group
28. sudo gpasswd <username> <groupname> -> Command is used to add a single user to a particular group
29. sudo gpasswd -M <user1>,<user2>,...,<usern> <groupname> -> '-M' flag allows to add multiple user in a particular group at once.
30. sudo usermod -aG <groupname> <username> -> Another way to append an existing user to a particular group.                             
    'a' flag means append the user and 'G' denotes group. Collectivty 'aG' denotes appends the user to the group G.
31. sudo userdel <username> -> To delete a user
32. man <command> -> give detailed information about the command and it's various use cases. 
                     eg. man gpasswd -> will give detailed overview of the command and its use cases.
33. printenv -> this command will give all the envirnment varibales details.
34. echo -e "hello\ndosto" -> it will print hello and dosto in two line \n denotes next line.
35. echo -e "hello\tdosot" -> it will print hello and dost with a space between them. -e is envirnment.
36. man zio -> Read about zip command, useful in backup
37. df -h -> Check disk space in Human-readable format
38. top -> Monitor CPU, memory, real time running processes 
39. ps -> List running processes and Can filter with grep to see specific process.
40. ps aux -> here 'aux' is  a flag which where;
       a denotes show all the processes of all the users. Without a it will show only current user/session processes
       u denotes Show the user/owner of the process and extra info like CPU%, MEM%, start time, etc.Makes the output “user-oriented”        
       with columns like USER, %CPU, %MEM.
       x Show processes that don’t have a controlling terminal. Includes background daemons/services, which a alone might not show.
41. xargs: Converts multi-line output into one single line. Replaces newlines with spaces
    eg. free -h | xargs,  df-h | xargs,  free -h | xargs | $10/$8

